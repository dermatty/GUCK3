{% extends "base.html" %}

{% block repstr %}
<div class="panel panel-default">
   <div class="container" style="text-align:center;">
       <img id="iframe2" class="img-responsive" src="{{ url_for('video_feed', camnr=camnr) }}">
   </div> {# container #}
   <div class="panel-body">
      <div class="row" >
          <div class="col-xs-2">
              <a href="#" class="btn btn-success btn-md" onclick="location.href='{{ url_for('livecam', camnrstr=camnr-1, ptz=1) }}'">
                  <span class="glyphicon glyphicon-arrow-up"></span>
              </a>
          </div>
          <div class="col-xs-2"> 
              <a href="#" class="btn btn-success btn-md" onclick="location.href='{{ url_for('livecam', camnrstr=camnr-1, ptz=2) }}'">
                  <span class="glyphicon glyphicon-arrow-down"></span>
              </a>
          </div>
          <div class="col-xs-2">
              <a href="#" class="btn btn-success btn-md" onclick="location.href='{{ url_for('livecam', camnrstr=camnr-1, ptz=3) }}'">web
                  <span class="glyphicon glyphicon-arrow-left"></span>
              </a>
          </div>
          <div class="col-xs-2">
              <a href="#" class="btn btn-success btn-md" onclick="location.href='{{ url_for('livecam', camnrstr=camnr-1, ptz=4) }}'">
                  <span class="glyphicon glyphicon-arrow-right"></span>
              </a>
          </div>
      </div> {# row #}
      <br>
      <table id="camurltable" class="table table-hover">
          <thead>
              <tr>
                  <th class="text-center">Camera</th>
                  <th class="text-center">Name</th>
                  <th class="text-center">Choose</th>
                  {# <th class="text-center">URL</th> #}
              </tr>
          </thead>
          <tbody id="camurltablebody">
              <form>
                  {% for camname, camurl, vurl in cameralist %}
                  <tr id="gaga"> 
                      <td class="text-center">{{ loop.index }}</td> 
                      <td class="text-center">{{ camname }}</td>
                      <td class="text-center">
                          <div class="radio">
                              <label><input id="camchangebu{{ loop.index }}" type="radio" name="optradio"{% if camnr == loop.index %}checked="checked"{% endif %} onclick="location.href='{{ url_for('livecam', camnrstr=loop.index-1) }}'"></label>
                          </div>
                      </td>
                      {# <td class="text-center">{{ camurl }}</td> #}
                  </tr>
                  {% endfor %}
              </form>
          </tbody>
      </table>
  </div> {# panel body #}

  <br><br>
</div> {# panel #}
{% endblock %}
    
